package settings;

import java.awt.*;

public interface Settings {

    // TextEditor's settings.
    Font DEFAULT_FONT = new Font("Arial", Font.PLAIN, 15);
    Dimension INITIAL_SIZE = new Dimension(500, 500);
    String INITIAL_DOCUMENT_TITLE = "Untitled.txt - TextEditor";
}
